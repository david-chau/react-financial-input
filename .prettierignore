# Generated by release-please on every release, in its own format. Prettier
# would reformat it, the bot would rewrite it, and the release pull request
# would fail CI forever.
CHANGELOG.md

# Build and tooling output.
dist
storybook-static
coverage
playwright-report
test-results

# Huge, machine-written, and npm owns the formatting.
package-lock.json
